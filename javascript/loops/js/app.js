// loops

for (let index = 0; index < 10; index++) {
    const element = index;
    console.log(element);
}

let i = 0;
while (i < 10) {
    console.log('while: ', i);
    i++;
}

// While: use for behaviors. Like when a pop-up tells user to click...until they do
// Behavioral-based conditions... use when you're waiting for a condition to be met


// Emmet Drillz
// for (let index = 0; index < array.length; index++) {
//     const element = array[index];
    
// }


// Emmet Drillz
// while (condition) {
    
// }


do {
    // do stuff at least once
} while (condition); // if true continue looping
